import TreeStore from '../../../js/tree/tree-store';
import { delay } from './kit';

// 节点状态
describe('tree:state', () => {
  describe('treeNode:checked', () => {
    it('todo', async () => {
      await delay(0);
    });
  });
});
