import TreeStore from '../../../js/tree/tree-store';
import { delay } from './kit';

// 节点延迟加载
describe('tree:lazy', () => {
  describe('treeStore:lazy', () => {
    it('todo', async () => {
      await delay(0);
    });
  });

  describe('treeStore:load', () => {
    it('todo', async () => {
      await delay(0);
    });
  });
});
